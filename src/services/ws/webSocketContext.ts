import { createContext } from "react";

// eslint-disable-next-line @typescript-eslint/no-explicit-any 
export const WSContext = createContext(null as any); 
//  创建一个 WebSocket 上下文，初始值为 null，类型为 any

